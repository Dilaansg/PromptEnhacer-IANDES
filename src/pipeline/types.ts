/**
 * @deprecated All types have been migrated to `@shared/types`.
 * This file is a temporary re-export shim and will be removed in a future cleanup.
 * Import directly from `@shared/types` instead.
 */
export type {
  LayerBOutput,
  LayerCOutput,
  PipelineConfig,
  PipelineResult,
  LayerBAnswers,
} from '@shared/types';
